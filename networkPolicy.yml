apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: services-policy
spec:
  podSelector:
  matchLabels:
    app: services
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: gateway